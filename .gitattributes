# Normalise line endings so the repo stays clean across Windows and Mac
* text=auto eol=lf

# Binary assets — never touch these
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.ico  binary
*.pdf  binary
